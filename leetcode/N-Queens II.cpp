1196 mili secs for large data setclass Solution {public:    int m,*board,result;    vector<string> oneSolution;    bool illegal(int i, int j){        for(int k=0;k<i;k++){            if(board[k]==j||(board[k]+k)==(i+j)||(k-i)==(board[k]-j))                return false;        }        return true;    }    void dfs(int num){        if(num==m){            ++result;            return;        }        for(int j=0;j<m;j++){            if(illegal(num,j)){                board[num]=j;                oneSolution[num][j]='Q';                dfs(num+1);                oneSolution[num][j]='.';            }        }        return;    }    int totalNQueens(int n) {        m=n,result=0;        board=new int[n];        memset(board,0,sizeof(board));        oneSolution.clear();        string s="";        for(int i=0;i<n;i++)            s+='.';        for(int i=0;i<n;i++)            oneSolution.push_back(s);        dfs(0);        return result;    }};